name: Share data between jobs

on: [push]

jobs:
  job_1:
    name: pratices
    runs-on: ubuntu-latest
    steps:
    -   name: 2
        run: echo "build_id=pankaj" >> $GITHUB_OUTPUT
        env:
            name: pankaj
            age: 40
    -   name: 3
        run: echo $GITHUB_OUTPUT

