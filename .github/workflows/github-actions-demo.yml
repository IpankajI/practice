
name: Share data between jobs

on: [push]

jobs:
    job1:
        
        runs-on: ubuntu-latest
        environment: dev
        steps:
            - run: echo ${{vars.name}} && echo ${{vars.NAME}}
