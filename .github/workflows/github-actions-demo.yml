name: Share data between jobs

on: [push]

jobs:
  job_1:
    name: pratices
    runs-on: ubuntu-latest
    steps:
    -   name: 1
        run: echo ${{format('Hello {0} {1} {2}!', 'Mona', 'the', 'Octocat') }}
    -   name: 2
        run: echo ${{ join(fromJSON('["push", "pull_request"]'), ', ') }}
