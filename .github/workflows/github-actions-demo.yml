name: Share data between jobs

on: [push]

jobs:
  job_1:
    name: pratices
    runs-on: ubuntu-latest
    steps:
    -   name: github.actor equal
        run: echo ${{contains('IpankajI', github.actor)}}
    -   name: pankaj==pankaj
        run: echo ${{pankaj==pankaj}}
