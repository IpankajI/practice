
name: Share data between jobs

on: [push]

jobs:
    job1:
        runs-on: ubuntu-latest
        environment: dev
        steps:
            -   name: env check
                env:
                    name: pankaj
                    age: ${{github.sha}}
                run: echo $name && $age
