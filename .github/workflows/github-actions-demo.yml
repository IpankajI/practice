
name: Share data between jobs

on: [push]

jobs:
  example_matrix:
    strategy:
      matrix:
        version: [10, 12, 14]
        os: [ubuntu-latest, windows-latest]
    run: echo matrix.version && echo matrix.os
